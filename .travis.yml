sudo: required
language: android
jdk: oraclejdk8
android:
 components:
    - platform-tools
    - tools
    - build-tools-26.0.2
    - android-26
    - extra-android-m2repository
before_script:
 - chmod +x gradlew
# change to build when lint issues are fixed
script:
- ./gradlew assembleDebug --stacktrace
